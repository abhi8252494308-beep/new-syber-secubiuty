# Core framework
fastapi==0.109.0
uvicorn[standard]==0.27.0

# Database
sqlalchemy==2.0.25
aiosqlite==0.20.0
alembic==1.13.1
asyncpg==0.29.0  # PostgreSQL driver for production

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9
cryptography==42.0.5  # Pre-built wheels available, no Rust needed
bcrypt==4.1.2

# Data validation - pin pydantic-core explicitly to avoid Rust compilation
pydantic==2.5.3
pydantic-core==2.14.6
pydantic-settings==2.1.0
python-dotenv==1.0.1

# HTTP client
httpx==0.26.0

# DNS resolution
dnspython==2.6.0

# PDF generation - use reportlab instead of weasyprint (no system deps)
# weasyprint==60.1  # Requires Cairo/Pango/GDK-PixBuf system libraries
reportlab==4.1.0
jinja2==3.1.4

# Utilities
validators==0.22.0
user-agents==2.2.0

# MongoDB
motor==3.5.0
pymongo==4.8.0

# Redis (optional)
redis==5.0.1